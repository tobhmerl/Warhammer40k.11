# Necron Army Builder — Master Plan & Resume Doc

> **Resume in a fresh chat:** read this file and `.github/copilot-instructions.md`, then continue from
> **§ Current status**. All spec context (originally chat Messages 1–3) is digested below so no prior
> conversation is needed.

## Architecture directive (non-negotiable)
Translate every requirement into the **existing** solution — do **not** add new projects, SQLite, or MAUI/native:
- **`Warhammer40k.11`** — Blazor WASM UI (net10), Necron dark theme already in place. Spec's "iOS sheets / swipe" → Blazor modal/bottom-sheet components + buttons.
- **`Warhammer40k.Api`** — Azure Functions isolated API (net9). Persistence = **Azure Table Storage** (repository pattern, see `ArmyRepository`). Catalogue seeding + roster CRUD live here.
- **`Warhammer40k.Core`** — shared POCOs + the **pure, unit-tested** validation engine (net9).
- **`Warhammer40k.Tests`** — xUnit (net10), references Core + Api.
- Auth = SWA built-in GitHub. The app **shell is public** so the SPA always boots and sign-in is a deliberate action on the Home page; `/api/*` requires the built-in **`authenticated`** role (except `/api/whoami`, which is anonymous so the landing page can detect sign-in state). The API derives identity from the SWA principal header and partitions Table Storage **per user**, so each account only ever sees its own data. (To make the site single-owner exclusive later: invite yourself as a custom `owner` role in the portal **and** gate `/api/*` on it, or add a server-side allow-list of your `userId` — don't lock `/*`, or the framework files 403 and the app can't boot.)

## Packet roadmap (AB1–AB9)
Each packet must build and keep tests green before the next.

| Packet | Scope | Projects | Gate |
|---|---|---|---|
| **AB1** | Catalogue domain POCOs + `CatalogueSeedLoader` (derivation) + loader unit tests | Core, Tests | builds, tests green |
| **AB2** | Embed `necron-catalogue-seed.json` + `CatalogueProvider` + `GET /api/catalogue` + client method + real-seed test (52 datasheets / 4 bindings + spot-checks) | Api, Core, UI, Tests | deployable |
| **AB3** | Tabbed nav + **Catalogue browse** tab (52 units grouped by role, searchable, detail sheet), themed | UI | deployable |
| **AB4** | `Roster`/`RosterUnit`/`Detachment`/`Enhancement` model + **rules R1–R11** + `RosterValidator` + rules tests + §6 integration test | Core, Tests | tests green |
| **AB5** | Roster persistence (Table Storage, per-user; evolves M1 `Army`) + `/api/rosters` CRUD + validate endpoint + client | Api, Core, UI | deployable |
| **AB6** | **Roster builder UI**: New-Roster wizard, editor (running points + validity pill, role sections, validation panel), Add-Unit picker (remaining copies), Unit configurator sheet | UI | deployable |
| **AB7** | **Catalogue editor** (Tab 2 authoring): CRUD + nested editors + wargear option-group authoring + referential-integrity checks | UI, Api | deployable |
| **AB8** | **Settings** tab: default points limit, theme, backup/restore (export/import JSON) | UI, Api | deployable |
| **AB9** | Wire **§10/§11** (detachment rules, per-enhancement eligibility, stratagems) → finalize R2/R6 + missing enhancement points | Core, Api, UI | complete |

## Current status
- **AB1 — done.** Catalogue POCOs (`Warhammer40k.Core/Catalogue/`), `CatalogueSeedLoader.Load`/`Enrich` (derivation), and loader unit tests (`Warhammer40k.Tests/CatalogueSeedLoaderTests.cs`, inline fixture covering every derivation branch). `dotnet build` + `dotnet test` green.
- **AB2 — done.** Deployable.
  - Seed embedded as `<EmbeddedResource>` in `Warhammer40k.Api` (`Seed/necron-catalogue-seed.json`).
  - `CatalogueProvider` (`Warhammer40k.Api/Catalogue/`) lazily loads + caches the enriched catalogue once; static `LoadEmbedded()` resolves the manifest resource by suffix so tests can reuse it.
  - `GET /api/catalogue` (`Catalogue` function, `Anonymous` — SWA gates `/api/*` to the `owner` role) returns the enriched `CatalogueData`; `CatalogueProvider` registered as a singleton in `Program.cs`.
  - Client: `IApiClient.GetCatalogueAsync` (Core) + `ApiClient` impl (UI) with empty-`CatalogueData` fallback matching the existing resilience pattern.
  - Real-seed test `Warhammer40k.Tests/CatalogueProviderTests.cs`: 52 datasheets / 4 bindings + derivation spot-checks (slug ids, copy caps, Overlord leader/Warlord eligibility, C'tan restrictions, every Monster → its binding, binding details, provider caching). **43 tests green.**
  - NEXT: **AB3** — tabbed nav + Catalogue browse tab (52 units grouped by role, searchable, detail sheet), themed.
- **AB3 — done.** Deployable.
  - Nav: added a **Catalogue** pill to `MainLayout` (Home · Catalogue · Armies).
  - Reusable `Components/Sheet.razor` (+ scoped CSS): accessible `role="dialog"` modal that renders as a bottom-sheet on mobile and a centered modal ≥720px (the spec's "iOS sheet"; reused by AB6). Closes via backdrop or button.
  - `Pages/Catalogue.razor` (+ scoped CSS): auth-gated load via `IApiClient.GetCatalogueAsync`; live search over name/keywords/faction-rules; all 52 datasheets grouped by `PrimaryRole` in battle order; themed unit cards open the detail Sheet (stat table, weapons table, cleaned ability text, leader targets, unit sizes, faction-rule/keyword chips, and the Pantheon binding surcharge for Monsters).
  - UI builds clean (0/0); 43 tests still green.
  - NEXT: **AB4** — `Roster`/`RosterUnit`/`Detachment`/`Enhancement` model + rules R1–R11 + `RosterValidator` + rules tests + §6 integration test (Core, Tests).
- **AB4 — done.** Tests green (Core + Tests only; not yet wired into UI/API — that's AB5/AB6).
  - **Models** (`Warhammer40k.Core/Rosters/`): `Roster` (header + 1250/1500/2000 presets + `NecronsFaction`/`DefaultPointsLimit` constants), `RosterUnit` (+ `WargearSelection`, `FromDatasheet` factory), `Detachment`/`Enhancement` + `DetachmentCatalogue.BuiltIn` — the 7 detachments with §8 enhancement points for **Starshatter Arsenal / Cryptek Conclave / Cursed Legion**; Hand of the Dynasty / Skyshroud Spearhead / The Phaeron's Armoury left empty until 11th MFM; **Pantheon of Woe** flagged `AppliesPantheonBindings`.
  - **Engine** (`Warhammer40k.Core/Rosters/Validation/`): pure `RosterValidator` runs **R1–R11** (one `IRosterRule` class each under `Rules/`) → `ValidationResult{Messages, TotalPoints, IsReady}` (Ready = no Error). Helpers: `RosterCalculator` (points = model-count `PointsOption` + enhancement + binding surcharge), `PantheonBindingApplier` (R10 auto-apply; idempotent + preserves an edited surcharge), `RosterValidationContext` (memoised catalogue/detachment/total resolution). Extracted shared `Warhammer40k.Core/Text/Slugger.cs` from `CatalogueSeedLoader` (identical output; AB1/AB2 ids unchanged).
  - **Deviations from §5** (documented inline): Warlord marked on `RosterUnit.IsWarlord` (no `warlordRosterUnitId`) and attachment via `RosterUnit.AttachedToRosterUnitId` (no `attachedLeaderIds`) — single source of truth so R5 can validate "exactly one" and R7 the co-leader rule.
  - **Permissive until §10/§11** (TODO in code): R6 enhancement→detachment membership only enforced once a detachment's `Enhancements` are authored; R7 Cryptothrall/Tomb-Crawler retinue augment; R8 wargear group min/max (option-groups unauthored); R11 intentionally empty (Info-only, none currently). R3 skips Epic Heroes so R4 owns their clear 0–1 message.
  - **Tests:** `RosterValidatorTests.cs` (per-rule R1–R11 on synthetic catalogues + `RosterCalculator` + `PantheonBindingApplier` + validator composition) and `RosterWorkedExampleTests.cs` (§6 worked example on the **real** seed: Hand of the Dynasty @2000 → Overlord 85 (Warlord+enh) + 2×Necron Warriors 90 + Nightbringer 340 = **605/2000 Ready**; 2nd Nightbringer → R4; Nightbringer as Warlord → R5). **83 tests green**; full solution builds 0/0.
  - NEXT: **AB5** — roster persistence (Table Storage, per-user; evolves M1 `Army`) + `/api/rosters` CRUD + validate endpoint + client (Api, Core, UI).
- **AB5 — done.** Deployable (full solution builds 0/0; **91 tests green**).
  - **Persistence** (`Warhammer40k.Api/RosterRepository.cs`): `IRosterRepository` + `TableRosterRepository` over a new **`Rosters`** table (auto-created), mirroring M1's army repo — per-user partition (`PartitionKey = userId`, `RowKey = rosterId`), Guid-`n` id on create, re-read after upsert. `RosterEntity` stores the unit list as a JSON string column (`UnitsJson`); `ModifiedUtc` = row `Timestamp`, `CreatedUtc` is a persisted column (defaulted on first save). Added **alongside** M1 `Army`/`Armies` (kept working) — Roster supersedes it; AB6 swaps the UI and Army can retire later.
  - **API** (`Warhammer40k.Api/Rosters.cs`): `/api/rosters` List/Get/Create/Update/Delete (same `ClientPrincipalReader` identity pattern — `userId` never from body/route; route id authoritative on update) + **`POST /api/rosters/validate`** which applies Pantheon bindings then runs `RosterValidator` against the server `CatalogueProvider` catalogue and returns a `ValidationResult`. Registered `IRosterRepository` in `Program.cs`. SWA already gates `/api/*` to `authenticated`, so functions stay `Anonymous`.
  - **Wire contract:** made `ValidationResult`/`ValidationMessage` JSON round-trippable (`[JsonConstructor]`, `JsonStringEnumConverter<ValidationSeverity>` so severity serializes as "Error"/"Warning"/"Info", `[JsonIgnore]` on derived `IsReady`/`Errors`/`Warnings`/`Infos`).
  - **Client** (`Warhammer40k.Core/IApiClient.cs` + `Warhammer40k.11/ApiClient.cs`): `GetRostersAsync`/`GetRosterAsync`/`SaveRosterAsync`/`DeleteRosterAsync` (same resilient empty/null fallbacks as armies) + `ValidateRosterAsync` → `ValidationResult?` (**null when the API is unreachable** so AB6 can fall back to local Core validation).
  - **Tests:** `RosterEntityTests.cs` (From→entity→ToRoster round-trip incl. units-through-JSON, blank-JSON fallback, ModifiedUtc) and `RosterValidationWireTests.cs` (result round-trip, severity-as-string, derived members omitted). Repo tests stay pure (no Azurite), matching the suite.
  - NEXT: **AB6** — Roster builder UI: New-Roster wizard, editor (running points + validity pill, role sections, validation panel), Add-Unit picker (remaining copies), Unit configurator sheet (UI).
- **AB6 — done.** Deployable (full solution builds 0/0; **91 tests green** — UI packet, no new tests).
  - **Nav swap:** `MainLayout` nav and the Home CTA now point at **`/rosters`** (replacing the M1 "Armies" link). Removed the superseded `Pages/Armies.razor`(+css); the `Army` model/`/api/armies`/client methods stay for later retirement.
  - **Rosters list** (`Pages/Rosters.razor` + scoped css, `/rosters`): auth-gated card list — each card shows detachment, computed `total/limit` (via local `RosterValidator`/`RosterCalculator`) and a **Ready/issues pill** — plus Duplicate (re-ids units and **remaps leader attachments**) and Delete, a `+` FAB, and a **New-Roster wizard** `Sheet` (name → fixed **Necrons** → points presets `1250/1500/2000` + custom → one of the seven `DetachmentCatalogue.BuiltIn`) that `SaveRosterAsync` → navigates to the editor.
  - **Roster editor** (`Pages/RosterEditor.razor` + scoped css, `/rosters/{Id}`): loads catalogue + roster, then **validates locally** on every edit (`PantheonBindingApplier.Apply` → `RosterValidator.Validate`) — no per-keystroke round-trip; `SaveRosterAsync` persists (keeps the in-memory instance, syncs server id/timestamps). Sticky header = editable name + running `total/limit` (turns red over limit) + validity pill + `Enhancements n/3` + Save (disabled unless dirty). Collapsible **validation panel** (severity-ordered R1–R11 messages). Units grouped into **role sections** (battle order) as cards with Warlord/→leader-target/enhancement/binding chips + points.
  - **Add-Unit picker** `Sheet`: searchable datasheets grouped by role, each showing **remaining copies = `MaxCopies − count`** and disabled at 0; adding keeps the sheet open. **Unit configurator** `Sheet`: size radios (`PointsOptions`), Warlord toggle (only when `WarlordEligible`, mutually exclusive across the roster), leader-attach `<select>` (targets = `LeaderTargetIds` ∩ roster units), enhancement `<select>` (from the detachment; note when none authored), read-only Pantheon binding/surcharge, and Remove (clears inbound attachments). Plus a **Summary/export** `Sheet` (text list in a selectable textarea).
  - Reused the existing `Sheet` component + theme variables; wargear option-groups intentionally deferred to AB7 (kept free/non-blocking, §R8).
  - NEXT: **AB7** — Catalogue editor (Tab 2 authoring): CRUD + nested editors + wargear option-group authoring + referential-integrity checks (UI, Api).
- **AB7 — done.** Deployable (full solution builds 0/0; **105 tests green**).
  - **Editable catalogue, per-user.** New `Warhammer40k.Api/CatalogueRepository.cs`: `ICatalogueRepository` + `TableCatalogueRepository` over a `Catalogue` table — the whole document is one entity per user (PartitionKey=userId, RowKey=`necrons`) with JSON **chunked** across `Json_n` props (the catalogue ~122 KB exceeds the 64 KiB property limit but fits the 1 MiB entity limit). `CatalogueChunking.Split/Join` + public `BuildEntity`/`ExtractJson` (unit-tested). The seed remains the immutable **default**: a user with no row reads the default; first save materializes their copy; reset deletes it.
  - **API:** `Catalogue.cs` now `GET /api/catalogue` per-user (saved ?? default; anonymous still gets default), `PUT /api/catalogue` (save, requires auth + ≥1 datasheet), `POST /api/catalogue/reset` (→ default). Registered `ICatalogueRepository`. Client gained `SaveCatalogueAsync` (throws to surface errors) + `ResetCatalogueAsync`.
  - **Core:** new `WargearGroup{Id,Name,Min,Max,Options}` / `WargearOption{Id,Name,PointDelta=0}` on `Datasheet.WargearGroups` (seed empty-but-present); **R8 now enforces** wargear (per group: known option ids + distinct count within Min..Max; Max=0 = unlimited). `CatalogueSeedLoader.Enrich` is **id-preserving** (slugs only when `Id` empty) so editing/renaming keeps roster references stable while other derived fields still recompute. New pure `CatalogueIntegrity.Check` (duplicate ids/names, missing sizes, wargear group/option name+id uniqueness + Min>Max, monster-without-binding, binding→unknown-unit).
  - **UI:** `Catalogue.razor` gained an **Edit catalogue** link → new `Pages/CatalogueEditor.razor` (`/catalogue/edit`): searchable datasheet list with New/Delete, a collapsible **integrity panel**, **Save** (dirty-gated) / **Reset** (confirm), and a datasheet editor `Sheet` with nested editors — name/role/flags, keyword & faction-rule chips, unit sizes, stat profiles, weapons (incl. comma keywords), abilities, and **wargear option-groups** (name/Min/Max + options). Edits re-`Enrich` + re-check integrity live. The AB6 Unit configurator gained a **wargear selection** section (toggle options per group with a choose-range hint) so R8's wargear rule is satisfiable.
  - **Tests:** wargear R8 (min/max/unknown option), `CatalogueAuthoringTests` (id-preserving enrich + recompute, integrity cases), `CatalogueChunkingTests` (lossless split/join + entity round-trip of a multi-chunk document). UI ships without bUnit (none in repo); guarded by full build + green suite.
  - NEXT: **AB8** — Settings tab: default points limit, theme, backup/restore (export/import JSON) (UI, Api).
- **AB8 — done.** Deployable (full solution builds 0/0; **110 tests green**).
  - **Settings persistence:** `UserSettings{DefaultPointsLimit, Theme}` + `AppThemes` (4 palettes: phosphor/arcane/ember/blood, `Normalize`) + `BackupBundle{Format, CreatedUtc, Settings, Catalogue?, Rosters}` in Core. `Warhammer40k.Api/SettingsRepository.cs` (`ISettingsRepository` + `SettingsEntity` + `TableSettingsRepository`, per-user single entity in a `Settings` table). Registered in `Program.cs`.
  - **API:** `Settings.cs` (`GET`/`PUT /api/settings`; anonymous GET → defaults). `Backup.cs` composes the three per-user repos: `GET /api/backup` assembles a bundle (settings + **customized** catalogue or null + all rosters); `POST /api/restore` replaces — saves settings, saves-or-resets catalogue, then clears and recreates rosters with fresh ids. Client gained `GetSettingsAsync`/`SaveSettingsAsync`/`GetBackupAsync`(nullable)/`RestoreBackupAsync`.
  - **Theme:** `--accent-rgb` added to `app.css` so accent tints/glows/backgrounds follow the palette; `[data-theme=…]` blocks for the 3 alternates. Tiny `window.tombworld` JS helper (`setTheme`, `download`) in `index.html`. Scoped `SettingsState` service loads settings once, applies the theme (via JS), exposes `DefaultPointsLimit`, and supports live preview / post-restore apply. `MainLayout` applies the theme on first render + gained a **Settings** nav link.
  - **UI:** `Pages/Settings.razor`(+css, `/settings`): default-points presets + custom, theme picker with **instant preview** + Save, and **Backup & restore** — Export (server bundle → JSON file download) and Import (`InputFile` → parse → confirm-gated destructive **Restore**). The New-Roster wizard now seeds its points from `SettingsState.DefaultPointsLimit`.
  - **Tests:** `SettingsAndBackupTests` (settings entity round-trip + theme normalization, `AppThemes.Normalize`, `BackupBundle` JSON round-trip incl. null catalogue).
  - NEXT: **AB9** — wire **§10/§11** (detachment rules, per-enhancement eligibility, stratagems) → finalize R2/R6 + missing enhancement points (Core, Api, UI).
- **AB9 — done.** 🎉 **Roadmap complete (AB1–AB9).** Deployable (full solution builds 0/0; **114 tests green**).
  - **Eligibility machinery finalized:** `EnhancementEligibility{RequiredKeywords[], ExcludedKeywords[]}` + `Enhancement.Eligibility`/`IsAvailableTo(Datasheet)` (all-required + none-excluded keywords; empty = unconstrained) and a `Stratagem{Id,Name,Type,CpCost,Text}` model on `Detachment.Stratagems`.
  - **R6 finalized** (`EnhancementRule`): now enforces per-enhancement **eligibility** (`IsAvailableTo`) alongside count/uniqueness/`CanTakeEnhancements`/membership — runs identically client-side (live editor validation) and on the server validate endpoint. **R2** doc clarified (R2 owns "exactly one detachment"; R6 owns enhancement membership + eligibility). A detachment with **no authored enhancements stays permissive** (the 3 without published points).
  - **UI:** the Unit configurator now offers **only eligible enhancements** (keeps a current pick visible; shows a "not eligible for this character" note); the roster editor's detachment name is a button opening a **Detachment-info Sheet** (enhancements with points + eligibility text, and stratagems) that degrades to "none defined yet".
  - **Scope note (no-op Api):** detachment definitions are static Core reference data (`DetachmentCatalogue.BuiltIn`) consumed identically by client and server, so finalizing R6 in Core finalized it everywhere — no new endpoint was needed. `DetachmentCatalogue` remains the single editable source for the **pending 11th-edition content** (the 3 detachments' enhancement points/names, exact per-enhancement eligibility keywords, and stratagem entries) — fill them in there to activate, **no engine change required**.
  - **Tests:** R6 eligibility (required-missing → block, required-present → allow, excluded → block) + `Enhancement.IsAvailableTo` matrix.
- **AB10 — done.** (Follow-up polish.) Deployable (builds 0/0; **114 tests green**).
  - Extracted the catalogue datasheet detail into a shared **`Components/DatasheetDetail.razor`**(+css) — stats/weapons/abilities/leader-targets/sizes/keywords + Pantheon binding (params: `Datasheet` + null-safe `CatalogueData`). `Catalogue.razor` now consumes it (no visual change; moved its detail CSS out).
  - The **roster builder's Unit configurator** gained a collapsed-by-default **"Datasheet"** section (toggle, reset per unit) rendering the same component, so you can see a unit's profile/weapons/abilities while list-building.
- **Seed regen — done.** Regenerated `Warhammer40k.Api/Seed/necron-catalogue-seed.json` from a NewRecruit 10th-ed roster export so every datasheet carries its **full weapon pool** + reconstructed **wargear option-groups** (26 datasheets / 32 either/or choices: Warriors flayer/reaper, Immortals gauss/tesla, Lychguard sword-shield/warscythe, Overlord 3 weapons). Converter `tools/convert_newrecruit_to_seed.py` (+ source export `tools/necron-roster-export.json`) is re-runnable; preserves `pointsOptions` + `pantheonBindings` from the prior seed. Still 52 datasheets / 4 bindings, so `CatalogueProviderTests` unchanged. Builds 0/0; **114 tests green**.
- **Play Mode (MVP) — done.** A phase-by-phase **battle command screen** to validate the catalogue drives play. Deployable (builds 0/0; **138 tests green**).
  - **Core (`Warhammer40k.Core/Play/`):** `BattlePhase` enum + `BattlePhases` (ordered/labels); `PhaseClassifier` (pure heuristics — Melee→Fight else Shooting; ability→phase by word-boundary cue matching so "remove"≠"move"/"discharge"≠"charge"; passive detection; invuln-save regex parse); `BattleRoster.Build` merges attached **Leaders into their bodyguard** (dangling = standalone), skips unresolved datasheets, exposes `BattleUnit`/`BattlePart` (split ranged/melee weapons, `AbilitiesIn(phase)` with passives surfaced under Command, wound-pool maths, `ParseWounds` null for variable wounds).
  - **UI:** `Pages/Play.razor`(+css, `/play`) roster picker → launch cards; `Pages/PlaySession.razor`(+css, `/play/{Id}`) the command screen — sticky header with **Round/CP steppers + alive-units counter + Reset**, a **phase tab bar** (per-phase content counts), and per-unit cards showing **phase-filtered weapons + abilities**, **in-memory per-unit wound trackers** (dead state at 0), and a full-datasheet `Sheet` (incl. attached leaders) via `DatasheetDetail`. **Play** nav link added; per-roster **Play** link on the Rosters list.
  - **Tests:** `PhaseClassifierTests` (10) + `BattleRosterTests` (14) — weapon/ability classification, boundary guards, leader merge, wound maths.
  - **MVP scope notes / follow-ups:** battle state is **in-memory** (resets on reload) — localStorage persistence is the obvious fast-follow; ability→phase is heuristic (text-driven) so a few abilities may land in an unexpected tab; vehicles show their first stat profile in the strip (full detail in the sheet). Possible next: localStorage round/CP/wounds, per-model damage tracks, stratagem prompts per phase (needs §11 content), objective/VP tracker.
- **Play Mode v2 (unified combat cards) — done.** Deployable (builds 0/0; **152 tests green**).
  - **Selected weapons only:** new pure `Warhammer40k.Core/Play/WargearResolver.SelectedWeapons(datasheet, unit)` returns always-on weapons + the weapons named by the unit's **setup wargear selections**; compound option labels ("Overlord's blade and tachyon arrow", "Claws and beamer") split on and/comma/&/+ and substring-match weapon names; **no selection → full loadout** (never empty). `WargearResolverTests` (5).
  - **One card per combat group, one view:** `BattlePart.Weapons/Ranged/Melee` now come from the resolver; `BattleUnit.CombinedAbilities` merges abilities across the group (primary first, then Leaders), **deduped by name**, each tagged with its source datasheet (`BattleAbility`). `PlaySession.razor` rewritten: every card shows a per-member **stat strip** (M/T/SV/W/LD/OC + ×models), **Ranged + Melee selected-weapon tables together** (source-tagged for grouped units), and the **combined abilities** list; Datasheet moved to a header chip-button.
  - **Phases are a lens, not a filter:** all units render in every phase ("assume every unit can act in all phases"); the phase tabs now just **highlight** the matching weapon section (`.table-wrap.phase-on`) rather than hiding units/weapons. Round/CP/wound trackers unchanged. `BattleRosterTests` extended (selected weapons + combined abilities).
- **Play Mode v3 (model counts, live attacks, collapsible cards) — done.** Deployable (builds 0/0; **170 tests green**).
  - **Per-part model counters (not pooled wounds):** tracking is now **models per part** (`_models` keyed by part id) with an inline ± counter at the **end of each unit's stat line**; a part hitting 0 greys its row, a group is "down" only when all parts are 0. The header shows the group's live model total.
  - **Live total attacks:** new pure `Warhammer40k.Core/Play/WeaponMath.TotalAttacks(models, A)` — a "Total A" column = current models × fixed A (20 Warriors × A1 = 20), updating as models drop; random A shown as a formula ("20×D6"). `WeaponMathTests` (12).
  - **Feel No Pain:** `PhaseClassifier.FeelNoPain` parse + `BattleUnit.FeelNoPain`; shown as an FNP chip. Header chips trimmed to **Warlord / Invuln / FNP** (dropped "N units", "N models", Datasheet); the datasheet moved to a "Full datasheet →" link in the card footer.
  - **Collapsible cards (less scrolling):** each card collapses to just its header line (chevron + name + chips + model total) via `ToggleCard`; a **Collapse all / Expand all** button sits in the sticky bar. In-memory like the other trackers.
  - **Follow-ups:** persist trackers (localStorage) so models/round/CP survive a refresh; optional per-weapon "to-hit/wound" helpers; objective/VP tracker.
- **Play Mode v4 (Core Stratagems + turn) — done.** Deployable (builds 0/0; **182 tests green**, +12 for this feature). Surfaces the universal §15 Core Stratagems "in the actual second of the game".
  - **Core (`Warhammer40k.Core/Play/`):** wired up the pre-existing `CoreStratagem` record + `BattleTurn`/`StratagemTurn` enums (the rulebook's green=Either / blue=Your / red=Opponent "Used in" markers) with a new static **`CoreStratagemCatalogue`** — `All` (authored verbatim, rulebook order) + `Usable(phase, turn)` which reuses `CoreStratagem.AppliesInPhase`/`AppliesInTurn` (empty phase set = any phase). Pure reference data (like `DetachmentCatalogue`), no API/storage. **Content complete — 10 stratagems:** Set 1 (15.02–15.06) Command Re-roll (Either/Any), Epic Challenge (Either/Fight), Insane Bravery (Your/Command, once-per-battle), Explosives (Your/Shooting), Crushing Impact (Your/Charge); Set 2 — all **Opponent's turn** — Rapid Ingress (15.07/Movement), Fire Overwatch (15.08/Movement), Smokescreen (15.10/Shooting), Heroic Intervention (15.11/Charge, +1CP "Into the Fray" mode noted in effect), Counteroffensive (15.12/Fight, 2CP). **15.09 Snap Shooting** is a referenced *rule* (no CP) folded into Fire Overwatch's effect; **15.01** is the section intro, not a stratagem.
  - **UI (`PlaySession.razor`):** new `_turn` state + a **Your turn / Opponent's turn toggle** pinned in the sticky header (opponent uses `--danger`); a contextual **Stratagems panel** under the phase tabs lists only `Usable(_phase, _turn)` — each card shows name/id/CP, flavour, and When/Target/Effect/Restrictions (newline-aware via `white-space: pre-line`), is dimmed when unaffordable, and has a **Use** button that spends its cost from the CP tracker (`StepCp(-cost)`). Empty-state when nothing applies.
  - **Tests:** `CoreStratagemCatalogueTests` (12) — any-phase Command Re-roll, your-turn gating (Explosives/Insane Bravery/Epic Challenge), opponent-turn gating (Counteroffensive), rulebook ordering, CP-affordability semantics, content integrity + unique ids.
  - **Open UX defaults (changeable on request):** inline panel (vs. slide-over), pure turn toggle (no round/phase side-effects), Use subtracts CP. The +1CP "Into the Fray" upgrade on Heroic Intervention is documented in its effect text; the Use button charges the base 1CP only.
- **Play Mode v5 (Army Rules) — done.** Deployable (builds 0/0; **192 tests green**, +10 for this feature). Surfaces the faction-wide first-level **Army Rules** (apply to every Necron army regardless of detachment).
  - **Core (`Warhammer40k.Core/Play/`):** new `ArmyRule` record (`Name`, `Faction` = `Roster.NecronsFaction`, `Text`, `Example`, `Lore`, `Phases` + `AppliesInPhase`/`AppliesInAnyPhase`/`HasExample`/`HasLore`) and static **`ArmyRuleCatalogue`** (`All` + `ForFaction(faction)`, case-insensitive). Pure reference data like `CoreStratagemCatalogue`, no API/storage. **Content:** **Reanimation Protocols** (Necrons, keyed to the **Command** phase) — verbatim body text (D3 wounds + the two reanimation bullets + the "nothing further happens" close) and the Lokhust Destroyers worked **Example**. **Lore** left empty (collapsed in source) — paste later to light up its toggle.
  - **UI (`PlaySession.razor`):** a collapsible **Army rules** card under the phase tabs (above the stratagems panel), reading `ArmyRuleCatalogue.ForFaction(_roster.Faction)`. Each rule shows name + body (newline-aware `pre-line`); **Example** and **Lore** are per-rule expanders (collapsed by default, rendered only when present); a `.now` accent highlight + phase chip appears when the current phase matches the rule's phase (Reanimation → Command).
  - **Tests:** `ArmyRuleCatalogueTests` (10) — faction filter (case-insensitive + other-faction empty), Command-only phase mapping, example/lore flags, authored-content + well-formedness + unique names.
- **Catalogue attach filter — done.** Deployable (builds 0/0; **145 tests green**).
  - **Core:** `CatalogueData.UnitsLedBy(leader)` (forward via `LeaderTargetIds`), `LeadersOf(unit)` (reverse scan), `AttachmentPartners(d)` (distinct **bidirectional** union, self-excluded, catalogue order). Pure + tested (`CatalogueAttachmentTests`, 7: inline logic + real-seed Imotekh→{warriors,immortals,lychguard} and Warriors←{overlord,imotekh}).
  - **UI (`Catalogue.razor`):** each unit card is now an **attachment-filter trigger** — clicking the card body narrows the list to the units it can be attached to/with (a Leader → its bodyguards; a bodyguard → its Leaders), with a banner ("Units **X** can attach to **N**" + Show-all) and an anchored-card highlight; re-clicking the anchor or Show-all clears it. A new **Datasheet** chip-button (next to Epic Hero/Monster, `@onclick:stopPropagation`) is now the only thing that opens the datasheet sheet. Keyboard-accessible (`role=button`/Enter/Space).
  - **Scope note:** "attach" = the **Leader** relationship (the only attachment dependency in the current data). Non-leader retinue dependencies (e.g. Cryptothrall) aren't in the seed; if added later, extend `AttachmentPartners`.
- **Setup polish — done.** Deployable (builds 0/0; **145 tests green**).
  - **Converter (`tools/convert_newrecruit_to_seed.py`):** added `clean_option_label` (strips a `<Unit> w/|with ` model-name prefix → clean weapon name, sentence-cased), `is_unit_name_leak` (singular/plural tolerant), and a `GENERIC_OPTION_LABELS` blocklist (`c'tan powers`, …). Regenerated the seed: **Triarch Praetorians** model-name leak gone (its duplicate group collapsed), **Tesseract Vault** "C'tan Powers" pseudo-group dropped, **Macrocytes / Tomb Crawlers / Wraiths** verbose "<Unit> w/ …" labels cleaned, **Tomb Crawlers** dup group collapsed. Wargear groups 32→29; still 52 datasheets / 4 bindings (tests unchanged).
  - **UI (`RosterEditor.razor`):** the roster unit card now shows the unit's **chosen wargear as chips** (new `WargearNames` resolver maps selected option ids → names; `.chip.wargear` style with a ⚔ glyph), so loadout is visible without opening the configurator.
- **Play Mode — Enhancements as abilities + phase-usable markers — done.** Deployable (builds 0/0; **269 tests green**, +5 for this feature).
  - **Core:** `Enhancement` gained `Text` + `StatModifiers` (+ derived `EffectSummary`) so a setup-assigned enhancement can render as a **live stat change** or **rules text**. `BattleRoster.Build` resolves each `RosterUnit.AssignedEnhancementId` against the selected detachments onto a new `BattlePart.Enhancement`; `BattleUnit.CombinedAbilities` appends it as a `BattleAbility` (new `IsEnhancement` + `AppliedSummary` — the latter now also carries leader-conferral summaries via `ConferredSummaryFor`), and `WeaponStatModifiers`/`UnitStatModifiers` apply the **bearer's** enhancement stat mods to its own model only. New `BattleUnit.IsAbilityActiveInPhase`/`ActiveAbilityCount` flag **text** abilities relevant to a phase (via `PhaseClassifier`), excluding always-on stat/applied abilities.
  - **UI (`PlaySession.razor` + css):** the bearer's card now lists its Enhancement (an **"Enhancement"** tag; a stat enhancement shows **"Applied: …"** and updates the live stat cells, a text enhancement is collapsible prose). Each **phase tab shows a count badge** of text abilities usable in that phase, and a phase-active text ability is highlighted with a **"Usable now"** marker (e.g. Imotekh's *Grand Strategist* lights up in the Command phase). Stat abilities are never phase-marked — they just change the stats while they apply.
  - **Content note:** built-in enhancements still carry name+points only, so an assigned enhancement surfaces its **name + tag** immediately; author `Enhancement.Text` / `StatModifiers` in `DetachmentCatalogue` to light up text/stat rendering — no engine change required.
  - **Tests:** `BattleRosterTests` (+5) — enhancement surfacing, stat buff + applied summary, weapon-buff scoped to the bearer, phase-active counting (excludes passives), and a stat enhancement never phase-marked even when its text names a phase.
- **Enhancement content + unit Upgrades — done.** Deployable (builds 0/0; **274 tests green**, +5 for this feature).
  - **Cryptek Conclave (4 enhancements):** authored `Text` + eligibility from the user's MFM wording — Atomic Disintegrators / Gravitic Bolas require **Cryptek**; Gauntlet of Compression / Quantum Abacus are unconstrained (any Necrons Character). They now show on the bearer's Play card (Atomic Disintegrators / Gravitic Bolas light up "Usable now" in the Shooting phase).
  - **Unit Upgrades (new model):** added `EnhancementScope { Character, Unit }` + `Enhancement.Scope`. **Hand of the Dynasty** now carries its two 11th-ed **Upgrades** — Enlivened Sentinels (NECRON WARRIORS, +20, Scouts 5") and Tools of Dominion (IMMORTALS, +15, [RAPID FIRE 1]) — assigned to whole non-Character units.
  - **R6 (`EnhancementRule`):** resolves the enhancement first, then a scope-appropriate target check — a Character Enhancement needs `CanTakeEnhancements`; a unit Upgrade must be a **non-Character** unit — then eligibility. **Configurator (`RosterEditor`):** `ShowEnhancementBlock`/`IsOfferable` offer Character enhancements to characters and Upgrades to matching units; the label switches **Enhancement ↔ Upgrade**.
  - **Tests:** `DetachmentCatalogueTests` (Cryptek text/eligibility + HoD unit Upgrades), `RosterValidatorTests` R6 unit-scope (allow on matching unit, block on a Character, block on missing keyword); updated the §6 worked example (Upgrade on a Warriors unit → **625**) and repointed the permissive-R6 test to Skyshroud Spearhead.
- **Live +6" Range buff (Gauntlet of Compression) — done.** Deployable (builds 0/0; **278 tests green**, +4 for this feature).
  - **Core:** added `StatTarget.Range` (a weapon characteristic; `StatMath.Apply` already adds inches to `24"`-style values) and `Enhancement.AffectsWholeUnit`. `BattleRoster` now applies an enhancement's `StatModifiers` **unit-wide** (every model in the combat group) when flagged, otherwise bearer-only (new `EnhancementStatModifiers` helper).
  - **Content/UI:** Gauntlet of Compression now carries `StatModifiers = [Range +6, Ranged]` + `AffectsWholeUnit`, so its bearer's whole unit (e.g. Technomancer + Wraiths) shows the increased **Rng** on each ranged weapon (Play card "Rng" cell wired through the buff pipeline + "Applied: +6\" Range" note). Melee Range stays `Melee`.
  - **Tests:** StatMath range inches, unit-wide enhancement reaches every model's ranged weapons (and not melee), and a catalogue pin for the Gauntlet modifier.
- **Play Mode + navigation polish — done.** Deployable (builds 0/0; **288 tests green**, +10 for this feature).
  - **Navigation:** deleted the Home page; **Catalogue is the landing** (`@page "/"`) and loads **anonymously** (default catalogue via a new anonymous GET `/api/catalogue` route rule; save/reset stay gated). Sign-in moved to **Settings** (Account section: identity + Sign out; signed-out card hosts the GitHub sign-in). `MainLayout` gained a **footer signed-in indicator** (whoami once on first render) and dropped the Home nav link.
  - **Play UI defaults:** the **Stratagems panel and all unit cards start collapsed** (collapse is the standard mode). Each (collapsed) card header shows a **"N usable" chip** = `ActiveAbilityCount(_phase)`, so you can see *which* units have phase-usable abilities without expanding (pairs with the phase-tab counts).
  - **Cleaner ability list:** the **Leader** ability (setup-only attach list) and the **Invulnerable Save** ability are hidden in Play (`HiddenInPlay`); abilities are ordered **text first, applied last**. Inv/FNP now render as **scoped chips** — `Inv 4+ · unit` (solid edge, incl. a Leader's conferral like **Master Chronomancer/Orikan**) vs `Inv 4+ · Overlord` (dashed edge, single model). Conferred FNP (**Technomancer → Wraiths**) already read unit-wide.
  - **Stat tracking:** weapon **stat cells are clickable single-select** (Rng/A/BS·WS/S/AP/D) — the chosen column highlights green so you can mark which step of an attack you're on.
  - **Core:** `PhaseClassifier.SaveScope` + `InvulnerableSaveScoped`/`FeelNoPainScoped`; `BattleUnit.InvulnerableSaves`/`FeelNoPains` (`SaveBadge` list, unit-wide best value + per-model badges, leader-conferral aware); `CombinedAbilities` filtering + text/applied ordering. Tests cover scope parsing, badge scoping, ability hiding, and ordering.
- **Turn-aware abilities + per-phase stratagem turn — done.** Deployable (builds 0/0; **299 tests green**, +11 for this feature).
  - **Rule:** "in your … phase" → usable in **your** turn only; "in your opponent's … phase" → **opponent's** turn only; an unqualified phase (e.g. "or the Fight phase") → **either** turn (the Fight phase happens in both turns).
  - **Core:** new `PhaseClassifier.TurnForPhase(text, phase)` reads the qualifier right before a "<phase> phase" mention (24-char look-back). `BattleUnit.IsAbilityActiveInPhase`/`ActiveAbilityCount` now take a `BattleTurn` and gate the "usable now" highlight + per-card/phase-tab counts by turn. `CoreStratagem`/`Stratagem` gained `UsableNow(phase, turn)` + a `StratagemTurn.Allows(BattleTurn)` extension: **single/any-phase stratagems keep their authored `Turn`** (all existing Core-stratagem tests unchanged), while a **2+ phase stratagem derives the turn per phase from its `When` text** (so "Your Shooting phase or the Fight phase" = your-shooting + either-fight). `CoreStratagemCatalogue.Usable` and the Play UI (stratagem filter + ability markers) use the new logic with the live turn toggle.
  - **Tests:** `TurnForPhase` qualifier cases, your/opponent ability gating, and multi-phase `UsableNow` (Molecular Targeting your-shoot/either-fight; Microscarab Swarm opp-shoot/either-fight).
- **Need-to-know stratagem keyword gate — done.** Deployable (builds 0/0; **312 tests green**, +7 for this feature).
  - **Rule:** a stratagem that targets a specific unit keyword is hidden in Play Mode when the army fields no unit with that keyword (e.g. **Smokescreen** needs SMOKE, **Explosives** needs EXPLOSIVES/GRENADES — Necrons have neither, so both vanish). Keyword-bearing stratagems Necrons *do* field (Epic Challenge → CHARACTER, Crushing Impact → MONSTER/VEHICLE, the Cryptek/Immortals detachment stratagems) still show when an eligible unit is present.
  - **Core:** `CoreStratagem` + detachment `Stratagem` gained `RequiredUnitKeywords` (default `[]` = any unit qualifies). Authored on Epic Challenge `[Character]`, Explosives `[Explosives, Grenades]`, Crushing Impact `[Monster, Vehicle]`, Smokescreen `[Smoke]`; detachment Cryptek-targeting stratagems `[Cryptek]`, Hand of the Dynasty `[Immortals]` / `[Immortals, Necron Warriors]`; army-wide NECRONS stratagems left unconstrained. `BattleRoster` now computes `ArmyKeywords` (case-insensitive set of every unit's datasheet keywords, built in the ctor) + `ArmyHasAnyKeyword(required)` (true when empty or any required keyword is fielded). `PlaySession.UsableStratagems` filters both Core and detachment stratagems through it.
  - **Tests:** authored `RequiredUnitKeywords` on the four Core + the detachment stratagems, `ArmyKeywords` collection (case-insensitive), `ArmyHasAnyKeyword` truth table, and the end-to-end "a typical Necron army hides Smokescreen + Explosives but keeps Epic Challenge + Command Re-roll".
- **Manual ability/stratagem scheduling (replaces all text heuristics) — done.** Deployable (builds 0/0; **302 tests green**). **Behaviour change:** the old text-guessing of an ability's phase/turn and the automatic application of leader conferrals / enhancement effects are **deleted**. Everything is now configured by hand in setup, default empty.
  - **Model:** new `AbilitySchedule { Key, Windows[AbilityWindow{Phase,Turn}], ApplyToUnit }` + `AbilityScheduleKeys` (ForUnitAbility = per-datasheet, ForArmyRule = per-name, ForEnhancement = per-id, ForCoreStratagem / ForDetachmentStratagem). `Roster.AbilitySchedules` + `GetOrCreateSchedule`/`FindSchedule`/`IsScheduledNow(key,phase,turn)`/`IsApplied(key)`.
  - **Engine:** `BattleRoster` carries the `Roster` (`Source`); `IsAbilityActiveInPhase` is purely schedule-driven (a window must be ticked); `BattleAbility` gains `Key`/`Windows`/`ApplyToUnit`/`ConferredSummary` with a computed `AppliedSummary` (non-null only when `ApplyToUnit`). Leader conferrals (weapon abilities, stat mods, unit abilities) and enhancement stat/weapon/shooting effects are gated behind `ApplyToUnit`. New `BattleRoster.CoreStratagemUsable`/`DetachmentStratagemUsable` = keyword eligibility AND manual schedule. Removed `PhaseClassifier.Classify`/`IsPassive`/`TurnForPhase` (kept weapon mapping + inv/FNP parsing), `BattlePart.AbilitiesIn`/`HasContentIn`, `CoreStratagem.UsableNow`/`CoreStratagemCatalogue.Usable`/`Stratagem.UsableNow`.
  - **Setup UI:** per-ability "When usable" 5×2 checkbox grid (+ "Apply [effect]" toggle for conferring abilities) in the unit config sheet; a new **Play timing** sheet schedules army rules, Core stratagems and detachment stratagems. **Play UI:** applied abilities render as the effect (no prose); a top-level "Army rules — usable now" band surfaces scheduled army rules. **Persistence:** `RosterEntity.AbilitySchedulesJson` round-trips schedules; backup carries them on the `Roster`.
  - **Tests:** rewrote the heuristic/auto-apply suites to drive off schedules (helpers `ApplyAbility`/`ApplyEnhancement`/`ScheduleAbility`); added `AbilityScheduleTests` (model/keys/Roster helpers), a "conferral not applied until ticked" test, schedule+eligibility stratagem tests, and `RosterEntity` schedule round-trip tests.
- **Roster editor auto-save — done.** Deployable (builds 0/0; **309 tests green**, UI-only, no new tests). `RosterEditor` now **debounced auto-saves** ~2s after the last change (every editor mutation funnels through `MarkChanged`), keeping the manual **Save** button (still showing Saving…/Save/Saved). `SaveAsync` is reentrancy-safe (skips when not dirty / already saving; re-marks dirty on failure to retry). Flushes pending changes on **in-app navigation** (`NavigationManager.LocationChanged`) and **tab-hide/close** (`tombworld.flushOnHide` → `visibilitychange`/`pagehide` → `[JSInvokable] FlushSave`); `IAsyncDisposable` does a final flush and tears the timer + JS listener down, so **Play Mode has zero auto-save activity** (each save is a one-shot HTTP request — nothing stays open).
- **11th-edition points (52 datasheets + 12 detachments + per-copy tiers) — done.** Deployable (builds 0/0; **344 tests green**, +35 for this feature). Captured the user's official 11th-ed MFM points.
  - **Per-copy tier pricing (new model):** added `Datasheet.EscalationRank` (0 = none) + `PointsOption.EscalatedPoints` (`int?`) so a datasheet can charge more from its **Nth unit** on. `RosterCalculator` is rank-aware: `UnitPoints(unit, datasheet, copyRank)` returns the escalated price when `copyRank ≥ EscalationRank`; `TotalPoints` groups roster units by datasheet to assign 1-based ranks; new public `CopyRank(roster, unit)` drives the editor display (per-unit points + size-picker option costs via `OptionPoints`). **16 tiered datasheets** authored (rank 2: Monolith 420/440, Obelisk 280/310, Seraptek 540/570, Canoptek Wraiths 95/115 + 220/240, Chronomancer/Technomancer 80/90, Tesseract Vault 445/465, Transcendent C'tan 340/360; rank 3: Ophydian/Deathmarks/Doomsday Ark/Lokhust Destroyers/Lokhust Heavy Destroyers/Skorpekh Destroyers/Tomb Blades/Triarch Stalker).
  - **Seed prices:** updated **all 52 datasheets** in `necron-catalogue-seed.json` to 11th-ed values (36 flat + 16 tiered) via a scoped, anchor-validated one-shot (since removed); JSON re-parses; still 52 datasheets / 4 bindings.
  - **Detachments (now 12):** added DP cost to the `Make` helper; filled enhancement points for **Skyshroud Spearhead** (1DP) + **The Phaeron's Armoury** (1DP) and added the **5 missing detachments** (Annihilation Legion 2DP, Awakened Dynasty 3DP, Canoptek Court 3DP, Hypercrypt Legion 2DP, Obeisance Phalanx 2DP) as **disabled, points-only** entries (no fabricated rules/stratagems — §10/§11 still pending). Cryptek Conclave / Cursed Legion / Hand of the Dynasty / Starshatter Arsenal already matched. **Pantheon of Woe** costs are seed Necrodermal Bindings (R10): reconciled **Singularity Matrix 55→45** to the card (other three already matched).
  - **Tests:** `PointsEscalationTests` (rank-aware pricing + grouped totals + independent per-datasheet ranking), seed round-trip of the new fields (`CatalogueSeedLoaderTests`), real-seed base + escalation pins + Singularity Matrix 45 (`CatalogueProviderTests`), the 12 detachments' DP + enhancement points (`DetachmentCatalogueTests`); updated the §6 worked example total **625→630** and repointed the permissive-R6 test to **Pantheon of Woe** (the only built-in with no Enhancements).
- **Still pending from user (content, not code):** the **11th-edition** values for **§10/§11** — per-enhancement **eligibility keywords** and **stratagems** for the detachments still authored points-only (Skyshroud Spearhead, The Phaeron's Armoury, Starshatter Arsenal, Cursed Legion, Annihilation Legion, Awakened Dynasty, Canoptek Court, Hypercrypt Legion, Obeisance Phalanx). Drop them into `DetachmentCatalogue` (`Eligibility.RequiredKeywords`/`ExcludedKeywords`, `Detachment.Stratagems`, and set `Enabled = true` to surface them in setup); R1/R2/R6 + the UI pick them up automatically. **Enhancement `Text`/`StatModifiers`/`Scope`/`AffectsWholeUnit` (Play Mode display + unit Upgrades) are authored in the same place.** *(Cryptek Conclave + Hand of the Dynasty are fully authored; all 12 detachments now carry DP + enhancement points.)*

## Derived-at-load fields (computed once by `CatalogueSeedLoader.Enrich`)
- `Id` = slug of name ("C'tan Shard of the Deceiver" -> `ctan-shard-of-the-deceiver`).
- `IsMonster` = keyword `Monster`. `IsUnique` = `IsEpicHero`.
- `MaxCopies` = EpicHero/Unique -> 1; Battleline or Dedicated Transport -> 6; else 3.
- `WarlordEligible` = `IsCharacter && !ability text "cannot be your Warlord"`.
- `CanTakeEnhancements` = `IsCharacter && !IsEpicHero && !ability text "cannot be given Enhancements"`.
- `HasLeaderAbility`, `AllowsCoLeader` (text "…already been attached"), `LeaderTargetIds` (match known unit names in the Leader ability text; tolerate `^^**`/`■` markup).

## Spec digest

### §1 Domain rules
Necrons only. Points presets **1250 / 1500 / 2000** + free custom (default 2000); same Strike-Force rules for all. Enhancements <= **3**. Copy limits: Epic Hero/Unique **1x**, Battleline & Dedicated Transport **6x**, else **3x**. Hard rules **block** "Ready"; soft rules **warn**; never auto-fix silently.

### §2 Flow
New Roster (name -> faction fixed Necrons -> points limit -> **one of 7 detachments**) -> live Roster editor -> Add Unit picker (grouped by role, shows remaining copies) -> Unit configurator (size/points, wargear, leader attach, enhancement, Warlord) -> Mark Ready / export.
7 detachments (11th-ed line-up): Hand of the Dynasty, Skyshroud Spearhead, The Phaeron's Armoury, Starshatter Arsenal, Cryptek Conclave, Cursed Legion, Pantheon of Woe.

### §3 Catalogue
**52 datasheets — seed-driven, never hand-typed.** `points` = smallest size; `pointsOptions` is the list of legal sizes. Wargear is **free** (cost = model count only; keep a `PointDelta = 0` for forward-compat).

### §4 Validation rules (each a small, independently unit-tested rule -> `ValidationMessage{Severity,Text,RosterUnitId?}`; Error blocks Ready)
- **R1 Points limit (E):** sum of unit points + enhancement points + Pantheon surcharges <= limit.
- **R2 One detachment (E):** exactly one; all enhancements/strats belong to it.
- **R3 Copy limits (E):** per datasheet caps (1 / 6 / 3). Surface remaining in picker.
- **R4 Epic Heroes 0-1 (E):** each named Epic Hero <= 1 (own clear message).
- **R5 Warlord (E):** exactly one; must be a Character and Warlord-eligible (block C'tan).
- **R6 Enhancements (E):** <=3; each once; Characters only, never Epic Heroes; one per Character; must come from the detachment and satisfy its eligibility constraint (structured data — *permissive until §10/§11*). Points -> R1.
- **R7 Leader attach (E/W):** target must be in the Leader's allowed list; one Leader per Bodyguard unless `AllowsCoLeader`; Cryptothralls/Tomb Crawlers retinue augment (max one, mutually exclusive) on a Cryptek-led unit; unattached Character = Info only.
- **R8 Unit size & wargear (E):** chosen size must be a valid `pointsOptions`; wargear group min/max — *unconstrained until option-groups authored*.
- **R9 Faction coherence (E):** every unit has `Faction: Necrons` (safety).
- **R10 Pantheon of Woe (E + auto-apply):** only when that detachment selected — every Necrons **Monster** takes its matching Necrodermal Binding and pays the surcharge (Deceiver/Singularity Matrix 55, Nightbringer/Quantum Goad 45, Void Dragon/Animus Damper 35, Transcendent C'tan/Reletavistic Tether 40; editable). Surcharge counts in R1.
- **R11 Battle-size minimums (Info only):** none currently; don't block.
Notes: points are by model count (never per-weapon); an attached Character still counts for R3/R4 and its points; "Ready" = no Error messages.

### §5 Roster data (store references, resolve catalogue at display)
- **Roster:** name, faction (Necrons), pointsLimit, detachmentId, createdUtc, modifiedUtc, computed totalPoints, warlordRosterUnitId, RosterUnits[], catalogueVersion.
- **RosterUnit:** datasheetId, chosen pointsOption (modelCount->points), selected wargear per group, attachedToRosterUnitId / attachedLeaderIds, assignedEnhancementId (Characters only), isWarlord, appliedBindingId, bindingSurcharge.

### §6 Screens (3 tabs) + worked example
Tabs: **Rosters** (card list, swipe/buttons, "+", New-Roster wizard, Roster editor with sticky header `1,910 / 2,000`, validity pill, `Enhancements 2/3`, role sections, unit cards, collapsible validation panel, Add-Unit sheet, Unit configurator, duplicate/rename/delete + summary/export); **Catalogue** (browse/search Factions->Detachments->Datasheets + Weapons/Abilities/Enhancements/Keywords libraries, full CRUD, referential integrity, fast entry); **Settings** (default points, theme, backup/restore, about).
**Worked example -> integration test:** detachment Hand of the Dynasty @2000 -> add Overlord (85, Warlord) + enhancement; x2 Necron Warriors (90); attach Overlord to a Warriors unit; add C'tan Nightbringer (340, can't 2nd, can't be Warlord); assert total <= 2000, exactly one Warlord, no cap breach, <=3 eligible enhancements, Ready only when all R-rules pass.

### §7 Seed field map -> model
`{ faction, datasheets:[{ name, points, primaryRole, isEpicHero, isBattleline, isDedicatedTransport, isCharacter, keywords[], factionRules[], statProfiles[{name,m,t,sv,w,ld,oc}], abilities[{name,text}], weapons[{name,type,range,attacks,skill,strength,ap,damage,keywords[]}], pointsOptions[{models,points}] }], pantheonBindings:[{name,unit,points}] }`. Derive the fields listed above at load.

### §8 Points / Pantheon
Size tiers + binding surcharges baked into the seed (`pointsOptions`, `pantheonBindings`). Enhancement points (10th MFM) for: **Cryptek Conclave** (Atomic Disintegrators 10, Gauntlet of Compression 20, Gravitic Bolas 15, Quantum Abacus 15); **Cursed Legion** (Cursed Circlet 25, Destroyer Ankh 20, Mark of the Nekrosor 20, Murdermind 15); **Starshatter Arsenal** (Chrono-impedance Fields 25, Demanding Leader 10, Dread Majesty 30, Miniaturised Nebuloscope 15); **Pantheon of Woe** (the four bindings). Hand of the Dynasty / Skyshroud Spearhead / The Phaeron's Armoury have no 10th points yet (fill from 11th MFM). Expose all points as **editable**.

### §12 Seeding
Idempotent `CatalogueSeeder`/loader consuming any `*-catalogue-seed.json`; de-dupe shared keywords/abilities; seed full weapon **pool**, leave wargear option-groups **empty-but-present** (authored later in the Catalogue editor). The Python converter `convert_newrecruit_to_seed.py` (regenerates the seed from a NewRecruit export) is a repo tool/reference only — keep under `tools/`, not part of the .NET build.

## Conventions
- File-scoped namespaces, `Nullable` enabled, `TreatWarningsAsErrors` in Core — keep it warning-clean.
- Validation engine is **pure** (no I/O) in Core so API + UI share it and it's unit-testable.
- Derive once at load; never re-parse ability text at runtime.
- Reuse the existing `ClientPrincipalReader`/repository + per-user partitioning patterns from M1.
